export enum NAVIGATIONLINKS {
  PRINT = "/prints",
  COVERAGE = "/coverage",
  VIDEOBOOKING = "/videoBooking",
  BLOG = "/blog",
  ACCOUNT = "/account",
  NOTIFICATION = "/notification",
  HOME = "/",
}
